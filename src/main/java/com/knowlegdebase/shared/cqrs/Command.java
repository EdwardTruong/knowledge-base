package com.knowlegdebase.shared.cqrs;

/**
 * Marker interface cho tất cả các Command không trả về kết quả.
 * Command được dùng để thay đổi trạng thái của hệ thống (Create, Update, Delete).
 */
public interface Command {
}
